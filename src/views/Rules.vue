<template>
  <h1 class="display-6 text-muted text-center mb-4">{{ $t('page-rules.title') }}</h1>

  <div class="text-center mt-4">
    <p>{{ $t('page-rules.rules.1') }}</p>
    <p>{{ $t('page-rules.rules.2') }}</p>
    <p>{{ $t('page-rules.rules.3') }}</p>
    <p>{{ $t('page-rules.rules.4') }}</p>
    <p>{{ $t('page-rules.rules.5') }}</p>
    <p>{{ $t('page-rules.rules.6') }}</p>
    <p>{{ $t('page-rules.rules.7') }}</p>
    <p>{{ $t('page-rules.rules.8') }}</p>
  </div>
</template>

<script>
export default {
  name: 'Rules'
}
</script>